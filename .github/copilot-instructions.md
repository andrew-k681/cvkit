See [AGENTS.md](../AGENTS.md) in the repository root.

That file is the single source of truth for how to work in this repo:
architecture, the command contract, the conventions that must not be broken
(lazy optional imports, never sourcing a `.env`, model-loading containment),
frame-naming traps, and what the tests do and do not cover.
